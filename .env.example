PORT=3005

# Inquiry Delivery Address (Where to send the project inquiry email)
TEMUR_INQUIRY_TO_EMAIL=hello@temur.studio

# SMTP Mail Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=no-reply@temur.studio
SMTP_PASS=your_smtp_password_here
SMTP_FROM="TEMUR STUDIO" <no-reply@temur.studio>

# Enable development mock submission if SMTP is not available locally
VITE_PROJECT_INQUIRY_MOCK=false
